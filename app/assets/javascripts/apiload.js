// when api loads, a function itereates through the images(objects) returned and checks to see if the object/image's ids are already saved in the database and if not, saves it so it is there when shortcode looks for it to display the images

// define api items
// rijksobjects = [response])
// define function
// function apiload(){
// 	// set up loop

// 	for(var i = 0; i < 10; i++){
// 		console.log(array[i])
// 	}
// }
