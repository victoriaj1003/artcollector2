<div class="navbar"> 
	<nav>

	<h1 class="logo"> art collector </h1>

	<%#= @nav_subtitle %>

	<div class="navlinks">

	<% view = controller.action_name %>
	<% if view != "dashboard" %>
		<%= link_to 'dashboard', dashboard_path %>
	<% end %>

	<% if view != "index" %>
		<%= link_to 'my stories', articles_path %>
	<% end %>

	<% if view != "edit" %>
		<%#= link_to 'edit story', edit_article_path %>
	<% end %>

	<% if view != "new" %>
		<%= link_to 'new story', new_article_path %> 
	<% end %>

	<% if view != "welcome#index" %>
		<%= link_to 'home', root_path %>
	<% end %>

	</div>


	

	</nav>
</div>